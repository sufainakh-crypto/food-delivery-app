const router = require("express").Router();

router.post("/", (req, res) => {
    res.json({ message: "Create order stub" });
});
router.get("/:id", (req, res) => {
    res.json({ message: "Get order details stub" });
});

module.exports = router;